doctype html
html
  head
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
  body
    h1 Pools
      each pool in pools
        div
          a(href=`/admin/pools/${pool._id}`)= pool.name
    